<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Entitlements for the Electron helper processes (Renderer, GPU, Plugin).

    Helpers get the same JIT and memory entitlements as the main bundle - the
    renderer is where V8 actually runs - plus `inherit`, which tells the
    hardened runtime to take the rest of its configuration from the parent.

    `inherit` cannot be combined with most other entitlements, which is why
    this is a separate, smaller file rather than a copy of the main one.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
